# Copy this file to .env and fill in secrets

# --- Core app ---
APP_PORT=8000
ALLOWED_ORIGINS=*

# --- Vector store selection ---
# chroma (built-in file-based) | pgvector (Supabase Postgres)
VECTOR_STORE_PROVIDER=chroma

# --- Chroma settings ---
CHROMA_PERSIST_PATH=./data/chroma
CHROMA_COLLECTION=prv-knowledge

# --- Supabase/Postgres (pgvector) ---
# Recommended: paste the full connection string from
# Supabase Dashboard → Settings → Database → Connection string → SQLAlchemy
# Example:
# DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@db.YOUR-REF.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=

# Alternatively, specify parts (used only if DATABASE_URL is empty):
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Optional Supabase keys (if you later use Supabase client elsewhere)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# pgvector behavior
# 384 for sentence-transformers/all-MiniLM-L6-v2; 1536 for OpenAI text-embedding-3-small
EMBEDDING_DIM=384
# cosine | l2 | ip
PGVECTOR_DISTANCE=cosine
PGVECTOR_IVFFLAT_LISTS=100

# --- Embeddings ---
# local | openai
EMBEDDING_PROVIDER=local
SENTENCE_TRANSFORMER_MODEL=sentence-transformers/all-MiniLM-L6-v2
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=

# --- LLM ---
# openai | ollama
LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini
OLLAMA_MODEL=llama3.1
